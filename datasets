import pandas as pd

df = pd.read_excel('sample.xlsx')

print(df.head(10))
df = df.drop_duplicates()
df = df.drop_duplicates(subset=['timestamp'], keep='first')
#print(df.duplicated().sum())  # Should print 0

print('testing if i can write anything i want here')

print('hellloow')